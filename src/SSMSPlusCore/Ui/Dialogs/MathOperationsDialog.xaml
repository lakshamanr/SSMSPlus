<Window x:Class="SSMSPlusCore.Ui.Dialogs.MathOperationsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ext="clr-namespace:SSMSPlusCore.Ui.Extensions"
        Background="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"
        mc:Ignorable="d"
        Title="Math Operations - Statistics"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12" ext:GridExtensions.Structure="auto,*,auto|*">
        <Grid Grid.Row="0" Background="Transparent" Margin="0,0,0,12" ext:GridExtensions.Structure="auto,auto|auto,auto,*">
            <Label FontSize="18" Padding="0" Margin="0,0,5,0" VerticalAlignment="Center">Grid Statistics</Label>
            <TextBlock Grid.Column="1" FontSize="11" VerticalAlignment="Center">Calculate <LineBreak/> Operations</TextBlock>
            <Separator Grid.Row="1" Grid.ColumnSpan="10"></Separator>
        </Grid>

        <Grid Grid.Row="1" ext:GridExtensions.Structure="auto,auto,auto,auto,auto,auto,auto,auto,auto|auto,*">
            <!-- Selection Info -->
            <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Total Selected Cells:</TextBlock>
            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" x:Name="TotalCellsText">0</TextBlock>

            <TextBlock Grid.Row="1" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Numeric Cells:</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" x:Name="NumericCellsText">0</TextBlock>

            <TextBlock Grid.Row="2" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,15">Non-Numeric Cells:</TextBlock>
            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,0,0,15" x:Name="NonNumericCellsText">0</TextBlock>

            <!-- Statistics -->
            <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,0,10"></Separator>

            <TextBlock Grid.Row="4" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Sum:</TextBlock>
            <TextBlock Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" x:Name="SumText">-</TextBlock>

            <TextBlock Grid.Row="5" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Average:</TextBlock>
            <TextBlock Grid.Row="5" Grid.Column="1" Margin="0,0,0,8" x:Name="AverageText">-</TextBlock>

            <TextBlock Grid.Row="6" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Median:</TextBlock>
            <TextBlock Grid.Row="6" Grid.Column="1" Margin="0,0,0,8" x:Name="MedianText">-</TextBlock>

            <TextBlock Grid.Row="7" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Min:</TextBlock>
            <TextBlock Grid.Row="7" Grid.Column="1" Margin="0,0,0,8" x:Name="MinText">-</TextBlock>

            <TextBlock Grid.Row="8" Grid.Column="0" FontWeight="Bold" Margin="0,0,10,8">Max:</TextBlock>
            <TextBlock Grid.Row="8" Grid.Column="1" Margin="0,0,0,8" x:Name="MaxText">-</TextBlock>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="CloseButton" Content="Close" Width="80" Height="26" Click="CloseButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
